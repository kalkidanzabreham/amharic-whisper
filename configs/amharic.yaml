model:
  name: "openai/whisper-small"   # start with small. upgrade to medium if WER is poor
  language: "amharic"
  task: "transcribe"

data:
  train_csv: "/content/drive/MyDrive/amharic-whisper/data/train.csv"
  val_csv: "/content/drive/MyDrive/amharic-whisper/data/val.csv"
  test_csv: "/content/drive/MyDrive/amharic-whisper/data/test.csv"
  audio_column: "audio_path"
  text_column: "text"
  sample_rate: 16000

training:
  output_dir: "/content/drive/MyDrive/amharic-whisper/checkpoints/whisper-amharic"
  batch_size: 8
  eval_batch_size: 8
  learning_rate: 0.00001
  warmup_steps: 500
  max_steps: 4000             # use steps not epochs for speech — more predictable
  gradient_checkpointing: true # saves VRAM — critical for Colab
  fp16: true                  # only if GPU available
  save_steps: 500
  eval_steps: 500
  logging_steps: 25
  dataloader_num_workers: 2

output:
  final_model_dir: "./whisper-amharic-final"